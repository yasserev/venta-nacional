{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/server.js" }
  ],
  "functions": {
    "server.js": {
      "maxDuration": 10
    }
  }
}
